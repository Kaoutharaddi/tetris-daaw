# URL del Backend.
# IMPORTANTE: Para probar en un solo ordenador, dejar como localhost.
# IMPORTANTE: Para probar en RED LOCAL (Móvil + PC), cambiar 'localhost' por la IP del PC (ej: 192.168.1.XX)
VITE_API_BASE_URL=http://localhost:8080